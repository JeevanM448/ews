# Render Blueprint - one-click deploy
# Deploy at https://dashboard.render.com/blueprints
services:
  - type: web
    name: ews-kerala-safeai
    runtime: python
    region: oregon

    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT

    envVars:
      - key: RECIPIENT_EMAIL
        value: jeevanelias1@gmail.com
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      # Add SMTP_USER and SMTP_PASS as secret env vars in Render dashboard
